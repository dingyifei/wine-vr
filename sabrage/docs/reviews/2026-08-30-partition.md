# Sabrage adversarial-review partition (design only)

Repo: `/Users/yifeiding/orca/workspaces/wine-vr/gui` (worktree, branch `dingyifei/gui`, base ce47c62). Scope: `sabrage/` (46,595 source lines incl. in-file tests) + parity spine. Line counts are `wc -l` of `.rs/.ts/.svelte` (tests live inline in `#[cfg(test)]`; no `tests/*.rs`, only fixtures). All paths below are relative to repo root; `S=sabrage/crates/sabrage-core/src`.

Global invariants every reviewer should hold (sources: root `CLAUDE.md` "Sabrage ⇄ demo.sh parity" + "Constraints that look wrong", `sabrage/PARITY.md` "Invariants that must NOT change", `contract/pipeline.toml` gate vocabulary, module docs):
- Artifact-byte parity is required; bug-for-bug parity is not. Bytes both sides write: host XR JSON (file form ends `}\n}\n`), `oxrsys-runtime.toml` first write (template verbatim, write-once), `cxbottle.conf` line `"CX_GRAPHICS_BACKEND" = "dxmt"`, `steam_appid.txt` (digits, no newline), `.sha256` marker (pin + exactly one `\n`).
- Every mutation goes through `Executor` (dry-run = same code path); checks are read-only probes; one process-wide `OPERATION_LOCK` (tokio, non-reentrant); `run` releases it at the launch boundary; permanent-vs-guarded boundary = run.sh line 179 (traps); persist-before-mutate for `session-state.json`; wineserver budgets 5 s fatal (run) / 4 s soft (stop); adb `forward --remove` per-serial for exactly 9943/9944, never `--remove-all`; `helper_is_arm64` word-match (`arm64e` alone fails); `win_path` trailing-slash rule; `WINEDEBUG` caller precedence; the six-line launch banner verbatim; exactly one privileged write (install layer 4).

## 1. Partition (14 areas)

### A1 `contract-parity-spine` (~2,734 Rust + ~950 shell/toml)
Files: `S/contract.rs` 339, `S/tap.rs` 106, `S/util/mod.rs` 468, `S/util/hash.rs` 65, `S/util/winpath.rs` 108, `sabrage/crates/sabrage-contract-gen/src/{lib.rs 343, main.rs 94}`, `sabrage/crates/sabrage-parity/src/lib.rs` 1211. Reference (read, don't own): `contract/pipeline.toml`, `contract/*.template`, `scripts/demo/contract.gen.sh`, `scripts/dev/parity.sh`, `scripts/dev/hooks/pre-push`, `sabrage/parity/shell.fingerprint`, `sabrage/PARITY.md`, `.github/workflows/parity.yml`, `scripts/demo/lib.sh`.
Attack:
1. `contract.rs` bakes the registry in via `include_str!` while `util::contract_hash` reads `repo_root` at runtime: a binary built from checkout X pointed at repo Y passes `meta.contract-sync` yet runs X's registry. Can tier-2 miss that?
2. `sabrage-contract-gen` re-declares only the fields the shell consumes: prove every scalar `lib.sh`/`run.sh`/`doctor.sh` read (`WIRED_PORTS`, `LEGACY_REVERSE_PORTS`, `DXMT_FILES`, `BS_APPID/DEPOT/MANIFEST`, `GBE_*`, `DXMT_TGZ_SHA256`, `HOST_XR_JSON`) is emitted, and that `--check` catches a header-only edit.
3. `shell_fingerprint_is_current` (lib.rs:124): file discovery vs parity.sh's `fingerprint_relpaths | sort` (locale `sort` vs Rust byte sort), a *new* `scripts/demo/*.sh`, a deleted one, CRLF; `PARITY_SKIP_FINGERPRINT` must skip, never pass/fail; can `--bless` bless a red tier-1?
4. `doctor_slug_coverage_matches_the_contract` (lib.rs:291) and tag tests (:374, :422): `logical_lines` continuation handling, slugs inside comments/strings, two slugs on one `# preflight:` line (run.sh:55, :69), and whether gate *kind* (autofix vs block) is checked, not just membership.
5. Artifact goldens (lib.rs:454–600): the host-manifest `expected` is built by the test's own substitution — does it share `render_host_manifest`'s escaping behaviour for a dylib path containing `"`/`\`/UTF-8? `toml_template()` vs on-disk; `win_path` table vs `winpath.rs` (`Z:` whole-path rule; prefix `None`).
6. `util`: `cmp_files` never "equal" on unreadable; `sha256_ok` case-insensitive vs marker `$(cat)` trailing-newline stripping; `helper_is_arm64` on `lipo` output `arm64e`, fat `x86_64 arm64e`, missing lipo; `bs_version` regex vs `grep -a -o -E -m1`.
7. `tap.rs`: `NotImplemented→skipped`; `--tap` append (`>>`) vs truncate; status vocabulary closed.

### A2 `core-primitives` (3,157)
Files: `S/paths.rs` 603, `S/executor.rs` 1327, `S/process.rs` 974, `S/error.rs` 152, `S/lib.rs` 101. Reference: `scripts/demo/lib.sh` (derived-path block, `fetch_pinned`, `install_if_changed`, `stop_wine`, `reap_stray`).
Attack:
1. `paths.rs:28 home_dir()` falls back to `/`: any writer then targets `/Library/Application Support/...`; `Paths::new` probes real `CrossOver.app` unconditionally (test nondeterminism, documented at `stages/stop.rs:665`); `resolve_repo_root` + symlinked/`..` roots vs the absolute dylib path embedded in the host manifest (install.sh uses non-canonical `$ROOT`) → byte mismatch → sudo thrash.
2. `executor.rs:516 write_atomic_real` (tmp+rename): tmp collision, cross-device rename, mode/owner of the replaced file (`cxbottle.conf`, `steam_appid.txt`), fsync, dir missing; `copy_if_changed` exec-bit preservation for the arm64 helper; `download` `.tmp` cleanup on *cancel* (not only curl failure/hash mismatch); `tar_xzf`/`dir_copy` (`cp -R` symlinks) argv; `touch` vs truncate-create semantics.
3. `spawn_detached`: `create_new` log, `process_group(0)`, `kill_on_drop(false)`, start-time capture fallback `0`; `RealExecutor` "refuses to mutate once cancelled" (run/mod.rs:43) — every method, including `run_child` racing the token.
4. `DryRunExecutor`: probes still execute — which ones? A dry-run `download` must never fetch; `planned()` ordering/`with_step` narrowing.
5. `process.rs`: `ChunkSplitter` (`\r\n` once, EOF partial flush, invalid UTF-8); `spawn_streamed` killpg SIGTERM→SIGKILL escalation + zombie reaping; `default_child_path` (Homebrew ahead of inherited `PATH` can shadow a user's toolchain); `find_processes_by_exe` on symlinked/relocated exe; `capture` with no timeout (a wedged `adb` freezes a GUI command); `ProcInfo::is_same_process` start-time granularity (pid recycled within a second).
6. `error.rs`: `Download`/`HashMismatch` Display byte-identical to `lib.sh`; `kind()` used for UI branching.

### A3 `checks-doctor` (4,431)
Files: `S/checks/{mod 702, config 581, bottle 371, build 350, system 317, bridge 271, host 258, overlay 213, network 206, game 200, headset 193, toolchain 182, pinned 178, sources 175, meta 116, audio 118}.rs` (NOT `run_only.rs` → A7). Reference: `scripts/demo/doctor.sh` (all 255 lines), `lib.sh`, `docs/troubleshooting.md` (quotes the strings).
Attack:
1. Read-only rule: any fs write, `adb`-daemon start, wine spawn, or lock acquisition inside `checks/`.
2. Tap parity of *skips*: whole-section skip guards (sections 3, 8, 10, 11) must tap `skipped` exactly where doctor.sh does; `silent_pass` slugs (`cx.present`, `bottle.named`, `game.present`, quiet `cfg.protocol.*`) suppressed on CLI but present on tap; `allow_adb_probes=false` → `hs.*` Skipped changes doctor's FAIL count vs zsh.
3. Verbatim message/remedy strings vs doctor.sh, including trailing-space quirks (`tr '\n' ' '` in bottle list, `net.ports` lsof join, `cfg.session-pins`).
4. `config.rs` header claims `serde_json` has no `preserve_order` while `sabrage-core/Cargo.toml` enables it and PARITY.md relies on it — which is true, and does multi-pin ordering match CPython insertion order? `manual_ips` absent vs `[]`; `protocol` parse: first-match `awk … exit` semantics vs Config.cpp last-wins.
5. Version compares (`sys.macos27`, `cx.version`): beta strings, `26.2.1`, missing `CFBundleShortVersionString`, `sw_vers` failure → find a real-looking string where zsh's `sort -V|grep -qx` and the native compare disagree.
6. `bridge.rs` ordered-substring scan vs `grep 'ActiveRuntime.*openxr.*wineopenxr64.json'` on `system.reg` (encoding, CRLF, escaped backslashes); `host.rs` serde vs python on BOM/duplicate keys/relative `library_path`/symlinked dylib; `pinned.rs` marker missing=WARN vs files missing=FAIL; `sha256_ok` on a directory.
7. `run_doctor` streaming + `operation_in_progress` annotation; exit code capped 255; `CheckCtx` bottle resolution order (section 3 load-bearing) when `--bs-dir` given without bottle.

### A4 `lock-and-fixes` (3,194)
Files: `S/stages/mod.rs` 732, `S/fixes/{mod 526, backend 715, adb 434, helper 395, session_json 392}.rs`. Reference: `run.sh` lines 37–52, 72–91, 113–124; `PARITY.md` (session.json remedy is *known broken*).
Attack:
1. `OPERATION_LOCK` (stages/mod.rs:434) non-reentrant: trace every preflight-autofix → whole-stage fix (`fix.run-build/install/setup`) path for a double acquire; `run_stage_holding_lock(None)` misuse; the lock is per-process — `sabrage` CLI vs GUI vs `demo.sh` share nothing (no file lock).
2. `forbidden_while_session_live` uses `live_session()` (this process only): a `demo.sh run` or detached session is invisible → `backend.rs` relies on `wineserver_alive` per bottle (TOCTOU with the CrossOver GUI rewriting `cxbottle.conf` on exit); `write_atomic` rename swaps the inode under a CrossOver that holds it open.
3. `backend.rs` three-branch edit vs run.sh:39–52 bytes: key in another section, different quoting/spacing, duplicate keys (sed rewrites all), CRLF, file lacking trailing newline (`printf '\n[EnvironmentVariables]…' >>`), `sed -i ''` last-line behaviour.
4. `adb.rs`: `forward --list` rows with IP:port serials; failed removal prints nothing (silent) — does `FixReport` still say `unchanged` honestly? `paths.adb=None` path.
5. `helper.rs`: verifies *source* arch, then `copy_if_changed` — is the *staged* result re-validated (run.sh:77 does)? exec bit; `$ENCODER_PROC` read from a toml that fails to parse.
6. `session_json.rs`: backup name collision/overwrite, `any_wineserver_alive` scope (non-CrossOver wine), `destructive` surfaced to GUI; test at :176–192 swaps process-global `HOME` under a tokio mutex — other tests in the same binary calling `home_dir()` can observe it.
7. `fixes/mod.rs` wire ids without `fix.` prefix; `DEFERRED_CONTRACT_FIX_IDS` (`fix.create-z-drive`) vs UI `contractFixIdToAction`; `EventSink` is a sync `Arc<dyn Fn>` — a slow sink stalls a stage.

### A5 `stages-setup-build-stop` (2,779)
Files: `S/stages/setup.rs` 725, `S/stages/build.rs` 998, `S/stages/stop.rs` 1056. Reference: `scripts/demo/{setup,build,stop}.sh`, `lib.sh` (`fetch_pinned`, `dxmt_ok`, `stop_wine`, `reap_stray`), CLAUDE.md "Pre-push arch gate" + "Three oxrsys build trees".
Attack:
1. setup: `rm -rf ext/dxmt-artifacts` → extract → completeness check → marker: crash/cancel windows leave no artifacts or a marker without files; Goldberg "present-but-unpinned kept, never re-fetched" leaves a truncated dll forever; dry-run skips postcondition dies and still emits `ok` rows (dishonest plan over a broken checkout); write-once toml when the file is empty/unreadable; `git submodule update` on a detached/foreign `ext/`.
2. build: tool-gate PATH == spawn PATH? `build-x64` configure must not pass/leave `OXRSYS_BUILD_ENCODER_HELPER=ON` (CMake FATAL gate, CLAUDE.md); helper tree thin-arm64 flags; staged helper re-validated after copy; cancellation mid-`ninja` (killpg) leaving a half-configured tree the next preflight self-heals from; `BUILD_OUTPUTS` seven vs doctor's `build.*` set; `cargo` resolved from `~/.cargo/bin` not Homebrew.
3. stop: 4 s soft wait vs run's 5 s fatal; survivor probe by argv substring (`Beat Saber.exe`) with `UpdateKind::Always`; reap by *exact* exe path misses a session launched from a different `repo_root`; `/bin/kill -TERM` per pid without identity re-check; `finish_stopped_session` swallows errors → step 4 reports a device it failed to restore?; `require_bottle` die text; `SwitchAudioSource` absent = silent.

### A6 `install-privilege` (2,473)
Files: `S/stages/install.rs` 1152, `S/privilege.rs` 1321. Reference: `scripts/demo/install.sh` (61 lines), `contract/active_runtime.x86_64.json.template`, PARITY.md "Install".
Attack:
1. Byte contract: `host_manifest_is_current` vs `$(<file)` (strips *all* trailing newlines) — a file with 0 or 2 trailing newlines: shell says current, native? Layer 4 renders only the comparison form; the write derives its own bytes from the dylib path (parity/lib.rs:493, install.rs:925).
2. Elevation: one `osascript … with administrator privileges` for `mkdir -p` + `install -m 0644 -o root -g wheel <tmp> <dest>`; `applescript_escape` on paths with `"`, `\`, `$`, non-ASCII (privilege.rs:835); staging file `0600` under `~/Library/Application Support/Sabrage/tmp` and `StagedTemp` Drop timing (removed while the elevated command still reads it? on panic?).
3. `AdminMethod::detect()` (stdin tty **or** `/dev/tty` opens → `sudo`): `tauri dev` from a terminal picks `sudo` inside the GUI; `sudo` wrong password → exact `sudo write failed` text; `NeedsAdmin` emitted before knowing the method → GUI says "macOS will ask" when `sudo` will.
4. TCC classification (`classify_write_error`, `is_inside_app_bundle` substring heuristic): EPERM vs EACCES, read-only volume, a repo path containing `.app/`; layer 1 half-applied overlay (d3d11 copied, dxgi failed) and the once-only `dxmt.stock-backup` never redone after a partial `cp -R`.
5. Layer 3 `wine … reg add` starts a wineserver install never stops (parity?) and the lazy `system.reg` flush → `bottle.registry` preflight blocks a launch right after a successful install; Warn-never-Fail re-probe.
6. Dry-run layer 4 prints "would prompt" instead of `NeedsAdmin` — but does it still stage a tmp file/plan the elevated spawns truthfully (privilege.rs:1080–1095)?

### A7 `run-preflight-actions` (3,404)
Files: `S/stages/run/preflight.rs` 1522, `S/stages/run/actions.rs` 1424, `S/checks/run_only.rs` 458. Reference: `run.sh` 6–153 and 219–270, `lib.sh` (`require_bottle`, `win_path`, `sha256_ok`), contract `[[check]]` gates + `[[launch_action]]`, PARITY.md "Run preflight"/"Run (launch)".
Attack:
1. State-machine order is Preflight → Reconcile: the preflight's *permanent* mutations (backend fix, helper restage, adb forward create/clear) run before `reconcile` refuses a `Live` session — contradicts PARITY.md:101 "before doing anything permanent"?
2. Gate application: Skipped → Fatal for every gated slug (PARITY:30) — including `warn` gates (`game.version`)? Autofix refusal (wineserver alive) → Fatal text vs run.sh:42 die; `Check` event carries the post-fix outcome; `AutoFixed` precedes it.
3. `read_toml_facts`: must equal `awk -F'"' '/^[[:space:]]*protocol[[:space:]]*=/{print $2; exit}'` (first match, double-quote split) while the runtime honours *last* wins (runtime_toml.rs header) — the preflight can validate a value the runtime ignores; single quotes, `protocol="alvr"`, key inside `[streaming]`, `encoder_process` unknown → warn + restage.
4. `dep.goldberg` hash-tolerance (warn, never block; missing dies); `run.wired-adb` with `unauthorized`/multiple devices; wired failure rollback removes both forwards then dies (run.sh:106–110).
5. `goldberg_stage`: `.orig-steam` backed up once — if the dll is already Goldberg the "original" is Goldberg (store/goldberg revert then lies); `cp` mode bits; `steam_appid.txt` via `write_atomic` (no newline, actions.rs:364); `steam_settings/*` truncate-create (`: >`), not touch; dll path fallback (`Beat Saber_Data/Plugins/x86_64` then root).
6. `wineserver-reset`: `-k` then bounded `-w` 5 s → warn listing `pgrep -lf wineserver` then die; native equivalent text/behaviour; adb-reverse-cleanup only with a serial; legacy branch unreachable (blocked earlier).
7. `launch_wine`: env table (`XR_RUNTIME_JSON`, `CX_GRAPHICS_BACKEND=dxmt`, `WINEDEBUG` caller precedence both branches, `SteamAppId`/`SteamGameId`), argv `--bottle <b> --no-update --cx-app <win>`, child `PATH`, `win_path` for a `bs_dir` outside `drive_c` (Z: with `bottle.zdrive` gate `none` → launches into a wine error); dry-run must run probes (`adb devices`) but never spawn wine.

### A8 `run-supervise-guards-logs` (4,017)
Files: `S/stages/run/mod.rs` 1884, `S/stages/run/guards.rs` 1192, `S/logs.rs` 941. Reference: `run.sh` 154–218, 249–270; `lib.sh stop_wine`; PARITY.md "Run (launch)"; CLAUDE.md `--wired` + log conventions.
Attack:
1. Lock handoff: `Option<OperationGuard>` dropped when the child is up; spawn failure *after* guards acquired → are guards released, `RUN_PHASE`/`RunPhaseScope` (mod.rs:277) cleared, `LIVE_SESSION` unset? Panic inside `guarded` → sync `Drop` fallbacks (`SwitchAudioSource` blocking on a runtime thread) and double-release guards.
2. `AudioGuard` (guards.rs:422 Drop): persist-before-mutate order; `SwitchAudioSource -c` parsing; prev == `BlackHole 2ch`; `fallback_output_device` when prev is gone (commit 2bf3661); volume 100 never restored (parity); `--no-audio`.
3. `DashboardGuard` (guards.rs:639): detached spawn, identity persisted, release kills by pid — `is_same_process` checked before `kill`? dashboard exits early → stale record.
4. Supervise: `select!` child ∥ cancel ∥ detach; cancel = `stop_wine` (`wineserver -k` + 40×0.1 s) then guards; normal exit leaves wineserver alive (parity); helper reap by exe path after exit; exit = wine rc; `Launched{pid, logPath}` timing; detach = `disarm` + record `detached:true`; wine stdout/stderr fd closed in parent.
5. `LIVE_SESSION` only reflects this process: GUI shows idle while `demo.sh run` streams (watcher freshness is the only hint) — dishonest states across the four launch routes.
6. `logs.rs`: `wine_log_candidate` local time + `-2` suffix detected via `create_new`; `Tailer` rotation = inode change **or** shrink (same-size truncate missed), partial-line buffering, CRLF, huge files; `list_past_runs` ordering; `oxrsys-runtime.log` rotation by the runtime; `get_log_source_path` with no repo root.

### A9 `session-reconcile-telemetry` (4,321)
Files: `S/session/{mod 648, reconcile 2004, state 357, watcher 1312}.rs`, fixtures `sabrage/crates/sabrage-core/tests/fixtures/phase3/*`. Reference: PARITY.md "Session (detach / reconcile)", `run.sh` traps 156–181, `stop.sh`.
Attack:
1. `classify` (reconcile.rs:196): `start_time 0` → IdentityMismatch; zombie pid; `Dead` → `RestoreMode::Full` "reaping by identity" — is the recorded dashboard/helper pid identity-checked or pid-only before `kill`? `SafeOnly` scope exactly = audio + forwards.
2. Single global `session-state.json` (no bottle/process scoping): CLI + GUI, or two bottles, clobber one record; `clear()` by a process that isn't the owner; `#[serde(default)]` forward-compat vs unknown-field loss on rewrite; atomic write path.
3. Live handling: PARITY:101 says Live refuses a new launch — does the GUI *adopt* (regain a `LiveSessionHandle` without a child handle)? What does Stop do then (cancel token vs `stop` stage)? `detached:true` + Live on next launch; `detach` racing a Stop in progress.
4. `finish_stopped_session` keeps the record until every guard is restored: `SwitchAudioSource` missing entirely → record kept forever, every stop/launch re-reports; device list parsing; restore reported as done when the child command exited non-zero?
5. Reconcile runs on Session-screen open and app start (mutating: restores audio, kills recorded pids) without confirmation and, in the GUI, without dry-run — audit `commands::reconcile_session` inputs (bottle `None`).
6. `watcher.rs`: `RUNTIME_STATUS_MAX_AGE` vs clock skew/future timestamps; `state` must stay opaque yet "Stalled only while streaming" (a5d128c) — how is streaming derived? `parse_encoder_ready` from a tail that may start at an *old* line (previous session's encoder chip shown as current); `runtime_status.process_id` vs recorded wine pid under CrossOver (`--cx-app` may exec through a different pid) → identity false-negative; `LIVE_SESSION` mutex poisoning; `RUN_PHASE` (mod.rs:242) set/cleared on every exit path.

### A10 `config-runtime-toml` (2,276)
Files: `S/config/mod.rs` 24, `S/config/runtime_toml.rs` 2252, fixtures `tests/fixtures/phase4/*.toml`. Reference: `contract/oxrsys-runtime.toml.template`, `scripts/demo/setup.sh` (write-once), CLAUDE.md "Config/state" (same-line `#` parser bug, `render_device` legacy-only), `ui/src/screens/Settings.svelte` `buildPatch` + `ui/src/stores/config.svelte.ts` (read-only for this area).
Attack:
1. Two readers (`toml_edit`, then a line-oriented fallback on parse failure): do they agree on which physical occurrence is "last" (runtime is table-blind)? Is a write allowed on the fallback path (`oxrsys-runtime.broken.toml`)? Dotted/quoted keys, inline tables, arrays, CRLF, non-UTF-8.
2. `apply_patch` (:738) edits the last physical occurrence and reports `shadowed`; same-line comment moved above the key (bytes outside the value change) — idempotence (patch twice), empty patch = zero-byte diff, comments on shadowed lines untouched.
3. `validate` (:592) accepted sets vs `ext/oxrsys/runtime/src/Config.cpp` (a value the GUI saves but the runtime silently ignores = dishonest "Saved"); float formatting (`client_sharpening`), bools, `protocol = "oxrsys"` writable though launch blocks it.
4. `write` (:999): rolling backups (`next_backup_path` :1087 same-second suffix, prune to 10 :1957) via executor before overwrite; backup of an unreadable/oversized file; create-if-absent from `toml_template` races `setup`'s own creation; live-session refusal uses `live_session()` (this process only).
5. `read` (:283) returns `exists=false` on permission-denied/IO error → UI offers to create → overwrites a real file? `RuntimeConfigView` `InvalidValue` surfacing for unknown values.

### A11 `ipc-boundary` (4,029)
Files: `S/events.rs` 747, `sabrage/src-tauri/src/{commands.rs 2071, lib.rs 257, main.rs 3, build.rs 3}`, `sabrage/ui/src/ipc.ts` 948. Reference: `sabrage/src-tauri/tauri.conf.json`, `capabilities/main.json`, PARITY.md "CLI / GUI" + Cmd-Q row. This area OWNS the hand-mirrored wire types; A12/A13/A14 read them.
Attack:
1. Mirror diff: every serde type crossing (`StageEvent` 12 variants + `kind` tag, `CheckStatus` incl. `not_implemented`, `FixAction` ids without `fix.`, `SessionStatus`/`SessionPhase`, `Reconciled`, `LogSource`, `RuntimeConfigView`/`InvalidValue`, `Settings`/`LaunchDefaults`, `GameEntry`/`GameValidity`/`GoldbergState`, `RuntimeStatus` snake_case exception) vs `ipc.ts` field names/casing/optionality; a variant missing from a TS union is dropped silently at runtime.
2. `RunRegistry` (:329) cancel closures keyed by run-id string: removed on completion? `cancel_stage` honest bool; `launch`'s promise lives for hours over a `Channel` — webview reload/HMR drops the channel while `LIVE_SESSION` persists → GUI idle, game running; two `run_stage` calls queue on `OPERATION_LOCK` while the UI shows both "running".
3. `SessionMonitorState` tokio mutex shared by the 1 Hz broadcaster (:953) and `get_session_status`: I/O under the lock; first broadcasts before the webview listens; `session://status` emitted to all windows.
4. `QuitApproved` (:843) lifecycle: Cancel resets it? Stop in progress at `RunEvent::Exit` → `detach_on_terminate` marks detached mid-teardown; `should_intercept_quit` when the frontend is not mounted → `prevent_close` with no dialog = unclosable window; `resolve_quit("stop")` awaits real teardown?
5. `stop_session` two branches (live handle cancel vs `stop` stage): resolves only after teardown? `revert_original_steam_dll`/`write_runtime_config`/`save_*` while a stage holds `OPERATION_LOCK` (store writes take no lock).
6. `load_settings` (:1202) silently defaults on a corrupt `settings.json` for `launch`/`get_repo_info` while `get_settings` errors — divergent truths; precedence GUI › `WINEVR_*` › settings (d41ec89) reads the GUI process env (a `tauri dev` shell leaks); tests mutate env under `WINEVR_BOTTLE_MUTEX` (:1657).
7. `TailRegistry` (:1065) leak on window close; capabilities/CSP (`unsafe-inline` styles, `dialog:allow-open` only) vs what `ipc.ts` calls; single-instance focus; `lib.rs` `RunEvent::Exit` does blocking I/O on the main thread.

### A12 `ui-shell-session` (3,498)
Files: `sabrage/ui/src/{App.svelte 110, main.ts 9, types.ts 6}`, `components/{Sidebar 191, NavItem 63, QuitDialog 109, GateModal 748, StagesPanel 236, CheckRow 130}.svelte`, `screens/{Session 732, Doctor 329, Logs 384}.svelte`, `stores/{session 221, stage 73, doctor 121}.svelte.ts`, `lib/demo.ts` 36. Reference: `ipc.ts` (read-only), `sabrage/docs/design/design-app.md`, PARITY.md "CLI / GUI".
Attack:
1. `session.svelte.ts`: `launch()` local phase vs `session://status`; cancel only while `preflight|launching` with `runId` (:109) — after `launched`, Stop must route to `stopSession`; status event arriving before `launch` resolves; `getSessionStatus()` on mount for reload recovery; module-level `listen` promises unawaited/double-subscribed under HMR; `quitRequested` reset.
2. `GateModal`: `cancel()` (:202) after `launched` fires the run's cancel token = kills the game with no confirmation? `fatal` dedupe (:82); `stageFinished` non-zero with no `fatal` row; `needsAdmin` text vs `sudo` method; Fix-from-Fatal opens a second gate over the single `stageStore.gate`.
3. `Doctor.svelte`/`doctor.svelte.ts`: `waiting` rows from a previous run that never report (removed slug) stay dim forever; `confirmFix` for the destructive `delete-session-json` says it is known-broken? doctor during a stage (annotation); `fixBusySlug` reset on error.
4. `Session.svelte`: bottle list from `getAppState`; launch-opts precedence with `settingsStore` defaults; `demoRunCommand` quoting (`bsDir` with `"`); controls disabled while a gate is open; navigation after launch.
5. `Logs.svelte`: `MAX_LINES` 5000 ring, `rotated` handling, tab switch start/stop tails (leak), `onDestroy`, autoscroll vs user scroll.
6. `QuitDialog`: `resolveQuit("stop")` failure leaves the dialog stuck; Escape = cancel; `Sidebar` `PHASE_DOT` covers every `SessionPhase`; `StagesPanel` Run/Dry-run enabled while a session is live; `App.svelte` `onMenu` launch/stop with no bottle selected.

### A13 `store-library-settings-ui` (4,128)
Files: `S/store/{mod 28, settings 280, library 910, goldberg 208}.rs`, `sabrage/ui/src/screens/{Settings 1091, EditGame 654, Library 502, About 210}.svelte`, `stores/{settings 85, library 88, config 72}.svelte.ts`. Reference: `ipc.ts`, `run.sh` 140–152 (Goldberg), design-app §4, CLAUDE.md "GUI-only state lives under `~/Library/Application Support/Sabrage/`".
Attack:
1. `settings.rs`/`library.rs`: corrupt file = hard Err — but Settings autosaves every field on change: does an autosave overwrite the corrupt file the user was shown? `#[serde(default)]` drops unknown fields on the next save (forward-compat loss); concurrent autosaves last-writer-wins; `repo_root` change invalidates the host manifest (absolute path) — warned?
2. `library.rs`: `save` upsert vs duplicate `bs_dir`; `remove` leaves disk artifacts; `effective_options` (:227) tri-state override (`Some(false)` beats global true) and whether the UI re-derives the merge (drift); `validate` (:317) cost per refresh (`bs_version` greps `globalgamemanagers`); `GoldbergState` by hash — "original" claimed without an original hash; `LastSession` writer and staleness.
3. `goldberg.rs` revert: `.orig-steam` may itself be Goldberg (A7 hypothesis 5) → "reverted" is a lie; hash verification; refuses while live (this process only); dry-run.
4. `Settings.svelte`: `buildPatch` sends only changed keys (`null` never sent); acknowledgement panel persisted; "Saved" flash on a failed save; Browse start dir via `suggestBsDir` and empty field showing the *derived* path as placeholder (c7637de) — user believes it's set; adb-probe toggle changes doctor tap results.
5. `EditGame.svelte`: debounced `validateGame` result applied to a newer draft (race); Add path from `newGameTemplate`; read-only Patches/Streaming from global config stale while open; navigating away mid-save.
6. `Library.svelte`: Run path duplicates `Session.svelte`'s `doLaunch`; validity only as fresh as last refresh (chips stale after a Run applies Goldberg); revert confirm; one-expanded-row state on refresh.

### A14 `cli` (2,140)
Files: `sabrage/crates/sabrage-cli/src/main.rs` 2140 (+ its `Cargo.toml` comment). Reference: `demo.sh` (58 lines: flags parsed *after* `CMD`, `exit 2` texts, `all` re-exec loop), `lib.sh` output helpers (`info/ok/warn/fail/die`), PARITY.md "CLI / GUI".
Attack:
1. Hand parser byte parity: `error: --bottle needs a name`/`unknown argument` + exit 2; flags before the subcommand (demo.sh takes `$1` as CMD first); `--flag=value` form; `--dry-run`/`--quiet`/`--tap` sabrage-only; `--tap` append vs truncate; relative paths.
2. `all`: in-process chain over fresh `StageCtx` with one token; `require_bottle` once; exit = first failing stage; Ctrl-C between stages; banner separator omitted (declared).
3. Renderer `stage_event_lines`: `Fatal` + `remedy:` continuation (7-space indent), `AutoFixed` → nothing, `NeedsAdmin`, `Output` passthrough vs `--quiet`, `Progress` `\r` chunks, colors gated on isatty + `NO_COLOR`, `error: <e>` suppression for `Fatal`/`TccDenied`/`AdminDeclined`, doctor exit = FAIL count capped 255, `WINEVR_DOCTOR_SOFT`.
4. Signals (:967–1010): raw `signal(2)` + polling thread; first INT/TERM cancels, second restores `SIG_DFL` for both and re-raises — exit 130 for TERM too (PARITY:74 says 130 always; re-raising SIGTERM under SIG_DFL yields 143); no trailing `error: cancelled` (a5d128c); Ctrl-C during the osascript prompt or mid-`all`; runtime shutdown with the detached wine child.
5. `sabrage run` prints the six-line banner verbatim; the audio child's output lines declared (a5d128c); `doctor` synchronous vs `#[tokio::main]`.

Seam ownership summary: `events.rs`+`ipc.ts`+`commands.rs` → A11 only; `util/*` → A1; `paths.rs`/`executor.rs`/`process.rs` → A2; `stages/mod.rs` (lock, `StageCtx`) → A4; `session/*` (incl. `state.rs`, `live_session()`) → A9; `logs.rs` → A8; `checks/run_only.rs` → A7 (its registry binding in `checks/mod.rs` stays A3); `checks/build.rs::helper_is_arm64` → A3 (re-exported by A1's util).

## 2. Hermetic verification (run after edits)

From `sabrage/` (workspace root, 5 members: sabrage-core, sabrage-cli, sabrage-contract-gen, sabrage-parity, src-tauri=`sabrage-app`):
- `cargo test --workspace` — all crates incl. `sabrage-app`'s 16 tests in `commands.rs` (needs the macOS Tauri toolchain; use `cargo test -p sabrage-core -p sabrage-cli -p sabrage-parity -p sabrage-contract-gen` to skip it).
- `cargo fmt --all --check` — no `rustfmt.toml`, defaults apply.
- `cargo clippy --workspace --all-targets -- -D warnings` — clippy is NOT configured or run in CI (no `clippy.toml`, no `[lints]`, `.github/workflows/parity.yml` only runs tier-1); one `#[allow(clippy::too_many_arguments)]` at `S/stages/run/mod.rs:345`. Establish a baseline before the fix phase and require "no new warnings".
From `sabrage/ui/` (node 24 per `sabrage/.mise.toml`; `npm ci` first):
- `npm run check` = `svelte-check --tsconfig ./tsconfig.json` (strict TS); `npm run build` = `vite build`. (`npm run tauri` = `cd .. && ui/node_modules/.bin/tauri` — a `tauri build` is not hermetic: signing/env-injected identity.)
Parity tier-1, from repo root:
- `scripts/dev/parity.sh --live=off` ≡ `cd sabrage && cargo test -p sabrage-parity -p sabrage-contract-gen` (also what `scripts/dev/hooks/pre-push` and CI run). `shell_fingerprint_is_current` (`sabrage-parity/src/lib.rs:124`) hashes `demo.sh` + every `scripts/demo/*.sh` except `contract.gen.sh` against `sabrage/parity/shell.fingerprint`; a red run means shell was edited — fix, then `scripts/dev/parity.sh --bless` (refuses if the rest of tier-1 fails). After any `contract/` edit: `scripts/dev/parity.sh --regen` (runs `cargo run -p sabrage-contract-gen -- --write`, tier-1, bless). `cargo run -p sabrage-contract-gen -- --check` alone verifies `contract.gen.sh` sync. Tier-2 (`--bottle`) reads live machine state and rebuilds `sabrage-cli`; not hermetic, not required for fixers.
Test-hygiene facts (grep-verified): every scratch helper uses `std::env::temp_dir()` + pid (24 sites, e.g. `S/stages/install.rs:387`, `S/config/runtime_toml.rs:1258`). `Paths::new(root)` derives `sabrage_appsup`/`oxr_appsup` from the real `$HOME` and probes real `CrossOver.app`; tests override the appsup fields (`S/stages/stop.rs:665–670` documents that omitting it would delete the developer's live `session-state.json`; `S/stages/run/mod.rs:1850`, `S/logs.rs:461`, `S/session/watcher.rs:631` override too). `S/fixes/session_json.rs:176–192` swaps the process-global `HOME` env var under a tokio `HOME_MUTEX` (other tests in the same binary reading `home_dir()` can observe the fake value — `S/privilege.rs:1263`, `S/paths.rs:545` assert with `ends_with`, so tolerant). `sabrage/src-tauri/src/commands.rs:1657` mutates `WINEVR_*` under a std mutex. No test writes under the real `~/Library/Application Support`: privilege staging under `sabrage_temp_dir()` is asserted only on dry-run plans (`S/privilege.rs:1088`, `S/stages/install.rs:936`). Reviewers of A2/A5/A7/A8 should still confirm every `RealExecutor` test site (`S/stages/run/actions.rs:1103`, `run/mod.rs:926,1850`, `install.rs:643`, `build.rs:923,962`, `config/runtime_toml.rs:1237`) keeps all `Paths` fields inside scratch.

## 3. Fix-phase file ownership

- Each area edits only its own file list above. A fix that needs another area's file is handed to that area's fixer (or serialized) — never cross-edited. Because the 14 lists are disjoint, all areas can run concurrently for behaviour-only fixes. Public-signature changes in the widely-imported areas (A2 `paths/executor/process`, A4 `stages/mod.rs`, A9 `session/*`, A11 `events.rs`/`ipc.ts`, A1 `util/*`) ripple into every stage/UI area: land those first (wave 1: A1, A2, A4, A9, A11), then the rest (wave 2: A3, A5–A8, A10, A12–A14). UI areas A12/A13 must not edit `ipc.ts`; a new wire field goes through A11 in the same change.
- Integration-only (lead merges; no area edits): `sabrage/Cargo.toml`, `sabrage/Cargo.lock`, `sabrage/crates/*/Cargo.toml`, `sabrage/src-tauri/Cargo.toml`, `sabrage/ui/package.json`, `package-lock.json`, `tsconfig.json`, `vite.config.ts`, `svelte.config.js`, `sabrage/.mise.toml`, `sabrage/src-tauri/tauri.conf.json`, `capabilities/main.json`, `contract/*`, `scripts/demo/*`, `scripts/dev/*`, `demo.sh`, `sabrage/parity/shell.fingerprint`, `sabrage/PARITY.md`, `sabrage/docs/*`, `CLAUDE.md`, `README.md`, `docs/*`, `.github/workflows/*`, `ext/*`. Rules: a contract edit needs `--regen` and its slug/gate lands in doctor.sh + `checks/` + preflight in the same commit; any change to a declared divergence needs a `PARITY.md` row; a shell edit needs `--bless`.
- Golden-tested bytes (a byte change requires updating the golden in the same change):
  - Host XR JSON: `S/util/mod.rs` `render_host_manifest`/`host_manifest_file_bytes` + `S/privilege.rs` `host_manifest_bytes` ← `sabrage-parity/src/lib.rs:454, :493, :527`; `S/stages/install.rs:925`; template `contract/active_runtime.x86_64.json.template`.
  - toml template: `S/util/mod.rs` `toml_template()` ← `lib.rs:536`; writers `S/stages/setup.rs:239`, `S/config/runtime_toml.rs:1026`; template `contract/oxrsys-runtime.toml.template`.
  - `steam_appid.txt`: `S/stages/run/actions.rs:364` ← `lib.rs:582`, `:814`.
  - `.sha256` marker: `S/util/mod.rs:294 contract_marker_bytes` ← `S/util/mod.rs:431`, `S/stages/setup.rs:411` (unit tests only; NOT in the parity crate).
  - cxbottle line: `S/fixes/backend.rs:29 TARGET_LINE` (+ `checks/bottle.rs` anchored grep) ← `backend.rs` tests, `checks/bottle.rs` tests; parity crate checks only the wine env `CX_GRAPHICS_BACKEND=dxmt` (`lib.rs:886`).
  - Launch text/argv/env/log-name goldens: `lib.rs:867` (wine env), `:926` (argv), `:968` (log stamp + `-2`), `:1027`/`:1088`/`:1146`/`:1185` (preflight die/warn, launch-action, guard, banner text verbatim in `run.sh`), `:545` (`win_path` table); contract regen `lib.rs:43`; registry order `:681/:714/:736`.
