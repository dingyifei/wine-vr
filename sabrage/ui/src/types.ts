// Shared frontend types for the Sabrage app shell.

/** The small state graph the main area switches on — one screen per sidebar nav item. */
export type Screen = "about" | "library" | "session" | "doctor" | "logs" | "settings";
